* EBM用fene表示共现概率，并让贡献概率最大优化fene参数，推理时，最大似然梯度下降优化目标样本
* 对噪声样本，根据MCMC采样，期望生成目标样本，因此添加
* LCD使得fene对正样本尽可能大，负样本尽可能小，
* 这样，fene具有对正样本较小、对负样本较大的特性，冻结fene，GD/Langevin Dynamics可迭代出目标样本
* 这样fene相当于判别器，为什么要用判别其来MCMC，而不是专门用一个生成器来从噪声生成样本呢
* EBM和